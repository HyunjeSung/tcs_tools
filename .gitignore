# tc_dashboard 실행 산출물 (앱별 runs_<app>/, latest_status_<app>.json 포함)
tools/tc_dashboard/runs/
tools/tc_dashboard/runs_*/
tools/tc_dashboard/latest_status.json
tools/tc_dashboard/latest_status_*.json
tools/tc_dashboard/__pycache__/
tools/tc_dashboard/ssh_control.sock
*.pyc

# python venv
venv/
.venv/

# 비공개 문서 (public repo 업로드 금지)
docs/AC Gen2 TestCase.xlsx

# 자격증명(대외비) — config.env와 달리 이 파일은 절대 커밋하지 않는다.
# tc_dashboard의 SSH 실행 시 여기 값을 앱별 PASSTHROUGH_ENV로만 원격 명령에 주입한다.
secrets.env
